name: session_cache
description: Cache data in local storage and invalid cache every time the app is opened
version: 0.1.0
author: Sandro Maglione
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  
  hive: ^1.4.4+1
  hive_flutter: ^0.3.1
  dartz: ^0.9.2
  uuid: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  hive_generator: ^0.8.2
  mockito: ^4.1.3

flutter:
